import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFFC4885A);      // Terracotta
  static const Color primaryLight = Color(0xFFE8C4A0); // Soft peach

  // Backgrounds
  static const Color background = Color(0xFFFDF6F0);   // Warm off-white
  static const Color surface = Color(0xFFFFFFFF);       // Card white
  static const Color surfaceAlt = Color(0xFFF5EDE4);   // Subtle warm tint

  // Text
  static const Color textPrimary = Color(0xFF2D2D2D);  // Near black
  static const Color textSecondary = Color(0xFF7A7A7A); // Muted grey
  static const Color textOnPrimary = Color(0xFFFFFFFF); // White on buttons

  // Status colors
  static const Color success = Color(0xFF6BAF8D);       // Soft green
  static const Color warning = Color(0xFFE8B84B);       // Warm yellow
  static const Color error = Color(0xFFD9534F);         // Soft red

  // Misc
  static const Color divider = Color(0xFFE8E0D8);
  static const Color shadow = Color(0x1A000000);
}